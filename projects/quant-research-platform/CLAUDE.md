# CLAUDE.md

Research-only repository. Never add broker connectivity or live-order code. Follow strict TDD: add a failing synthetic-data test, run it, then implement. Never make online downloads unit-test prerequisites. Keep all services bound to 127.0.0.1 and never print secrets or environment files.
