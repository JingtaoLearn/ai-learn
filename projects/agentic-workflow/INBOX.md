# Inbox

- Jingtao reset the project to functional validation: simplify everything, use AI Agents as nodes, keep the Goal in one fixed file, build active information flow, and remove heavy code and all tests.
- Jingtao aligned ownership around one isolated Agent suite per product, one canonical persistent Product Owner Session as its brain, dynamic Matt Skill selection, and real-time information-event Signals as the final trigger.
