# Inbox

- Jingtao reset the project to functional validation: simplify everything, use AI Agents as nodes, keep the Goal in one fixed file, build active information flow, and remove heavy code and mandatory test infrastructure.
- Jingtao aligned ownership around one isolated Agent suite per product, one canonical persistent Product Owner Session as its brain, dynamic Matt Skill selection, and event-driven information flow.
- Jingtao established Goal, Principles, and Validation as the three core elements and required Default, Product, Role, and Task principle inheritance rather than Owner-only judgment.
- Jingtao clarified that the entire current environment is prototype validation, including services deployed on `zhlearn`: show the user a reversible working capability and validate its actual effect before broad formalization, hardening, or large test suites.
