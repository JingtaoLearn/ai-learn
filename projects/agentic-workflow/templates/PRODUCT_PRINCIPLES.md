# Product Principles Template

Keep this file product-specific and short. It is owned by Jingtao and constrains every Role serving the product.

## Stable product principles

1. `<Durable value or truth priority>`
2. `<Product-specific evidence standard>`
3. `<Boundary between research, product behavior, and external effect>`
4. `<What counts as a valid negative or stop outcome>`
5. `<Product-specific user or risk boundary>`

## Explicit overrides of Default Principles

`None`, unless Jingtao explicitly approves an override.

For every override record:

- Default Principle replaced: `<number and title>`
- Replacement: `<exact product rule>`
- Reason: `<why the product requires it>`
- Owner: `Jingtao`
- Effective revision: `<content hash or version>`

## Exclusions

Do not put current priorities, host names, Session IDs, Issue numbers, commands, retries, deployment steps, parameter values, or temporary task state here. Put those in Goal, State, Execution contracts, authorization records, or Handoffs.
