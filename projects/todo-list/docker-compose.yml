services:
  todo-app:
    build: .
    container_name: todo-app-issue11
    restart: always
    environment:
      - VIRTUAL_HOST=todo-issue11.${S_DOMAIN}
      - VIRTUAL_PORT=80
      - LETSENCRYPT_HOST=todo-issue11.${S_DOMAIN}
    expose:
      - "80"
