# Domain for the service (e.g., example.com)
S_DOMAIN=example.com

# Email for Let's Encrypt SSL certificates
S_EMAIL=admin@example.com

# Persistent storage path for Docker volumes
S_CONTAINER_FOLDER_STATIC=/data/static

# Application settings
BASE_URL=https://board.example.com
PORT=3000
DATA_DIR=/app/data
